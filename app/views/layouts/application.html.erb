<!DOCTYPE html>
<html>
  <head>
    <title>PlaySpace</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Architects+Daughter|Bungee|Caveat|Chewy|Indie+Flower|Shadows+Into+Light+Two" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-custom">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= image_tag("playspace-logo.png", height: '100px', class: 'navbar-brand')%>
        </div>
        <ul class="nav navbar-nav navbar-right navbar-collapse collapse">
          <% if current_user %>
            <li><%= link_to "Account", "#", class: 'btn yellow-btn bold-btn' %></li>
            <li><%= link_to "Sign Out", destroy_user_session_url, method: :delete, class: 'btn yellow-btn bold-btn' %></li>
          <% else %>
            <li><%= link_to "Join Us", new_user_registration_url, class: 'btn yellow-btn bold-btn' %></li>
            <li><%= link_to "Sign In", new_user_session_url, class: 'btn yellow-btn bold-btn' %></li>
          <% end %>
        </ul>
      </div>
    </nav>
    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
    <footer class="footer">
      <div class="container">
        <p>some text here</p>
      </div>
    </footer>
  </body>
</html>
